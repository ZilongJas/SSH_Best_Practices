can work together to "share" a terminal
- `sudo apt install screen`
- `screen` to start a session
- `screen -list`
  - list existing session
  - `screen -x [session name]` to join
- ctrl + A, ctrl + D to deattach
- `exit` to exit
